name: pixelshare
description: PixelShare - Create. Share. Pixel.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  share_plus: ^10.1.4
  http: ^1.2.0
  shared_preferences: ^2.3.2
  flutter_colorpicker: ^1.0.3
  cupertino_icons: ^1.0.6
  flutter_local_notifications: ^17.2.3
  workmanager: ^0.6.0
  sensors_plus: ^6.1.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
